<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Professional Dashboard</title>
    <!-- Bootstrap CSS -->
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css" rel="stylesheet">
    <script src="https://cdnjs.cloudflare.com/ajax/libs/Chart.js/3.7.0/chart.min.js"></script>
    <link rel="stylesheet" href="css/<%= content %>.css">
    <link rel="shortcut icon" type="image/png" href="images/favicon.ico"/>
    <!-- Custom CSS -->
    <style>
        /* Custom styles for a professional look */
        body, html {
            height: 100%;
            margin: 0;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }

        .sidebar {
            background-color: #343a40;
            color: #fff;
            min-height: 100vh;
            padding-top: 20px; /* Add padding top to create space for company logo */
        }

        .sidebar-header {
            text-align: center;
            margin-bottom: 20px;
        }

        .sidebar-header img {
            width: 80px;
            margin-bottom: 10px;
        }

        .sidebar-header h3 {
            font-size: 1.5rem;
        }

        .sidebar .nav-link {
            color: #adb5bd;
            transition: color 0.3s ease;
        }

        .sidebar .nav-link:hover,
        .sidebar .nav-link.active {
            color: #fff;
            background-color: #495057;
        }

        .content {
            padding: 20px;
        }

        /* Responsive layout */
        @media (max-width: 768px) {
            .sidebar {
                min-height: auto;
                width: 100%;
                position: relative;
            }
            .content {
                margin-left: 0;
            }
        }
    </style>
</head>
<body>
    <div class="container-fluid">
        <div class="row">
            <!-- Sidebar -->
            <div class="col-md-3 bg-dark sidebar">
                <div class="sidebar-header">
                    <h3 class="border-0 font-weight-bold text-white"><a href="/dashboard" class="text-white">Dashboard</a></h3>
                </div>
                <ul id="sidebarNav" class="nav flex-column">
                    <li class="nav-item">
                        <a class="nav-link <%= content === 'dashboard' ? 'active' : '' %>" href="/dashboard">Dashboard</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link <%= content === 'users' ? 'active' : '' %>" href="/users">Users</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link <%= content === 'categories' ? 'active' : '' %>" href="/categories">Categories</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link <%= content === 'posts' ? 'active' : '' %>" href="/posts">Posts</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link <%= content === 'contacts' ? 'active' : '' %>" href="/contacts">Contacts</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link <%= content === 'newsletter' ? 'active' : '' %>" href="/newsletter">Newsletter</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/logout">Logout</a>
                    </li>
                </ul>
            </div>
            <!-- Content -->
            <div class="col-md-9 content">